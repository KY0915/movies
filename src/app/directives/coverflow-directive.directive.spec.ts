import { CoverflowDirectiveDirective } from './coverflow-directive.directive';

describe('CoverflowDirectiveDirective', () => {
  // it('should create an instance', () => {
  //   const directive = new CoverflowDirectiveDirective();
  //   expect(directive).toBeTruthy();
  // });
});
